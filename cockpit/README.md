# cockpit — native bento deck

swiftui deck bound to the daemon over ws `127.0.0.1:7171`.

```sh
swift run
```

optional xcodegen bundling:

```sh
xcodegen generate
open OrchestratorCockpit.xcodeproj
```

see `../docs/ARCHITECTURE.md` for the ipc contract.
